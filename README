本项目为模拟在线OJ，拥有基本的判题能力，多组用例检测，支持非acm模式
包含三个核心模块和两个次要模块
核心模块一：网页服务器
负责收发并处理网页请求，核心技术栈：协程，tcp套接字，http协议，mysql数据库，jsoncpp库
核心模块二：编译服务器
负责处理代码的编译，运行和校验，核心技术栈：tcp套接字，select，jsoncpp库，管道通信，boost无锁队列
核心模块三：前端界面
负责渲染接收的数据，核心技术栈：html，css，javascript
次要模块一：日志模块
负责记录运行信息，核心技术栈：C++20format库，CRTP
次要模块二：数据库内容填充
